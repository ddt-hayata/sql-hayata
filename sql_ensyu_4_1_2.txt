/* 
 * 注文明細テーブル（order_desc）を新規に作成してみましょう。
 * 以下の空欄を埋めて、SQL命令を完成させてください。
 */
 
 // 問題
 ➀
  order_decs
  (
      po_id INT NOT NULL,
      p_id CHAR(10) NOT NULL,
      ➁,
      ➂(po_id, p_id)
  )
  ;

 // 回答
 CREATE TABLE
  order_decs
  (
      po_id INT NOT NULL,
      p_id CHAR(10) NOT NULL,
      quantity INT,
      PRIMARY KEY(po_id, p_id)
  )
  ;