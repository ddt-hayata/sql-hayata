/* 
 * 貸出記録テーブル（rental）上を検索し、これまでに1度も書籍の貸出を行ったことのない
 * ユーザの氏名をユーザテーブル（usr）から取り出してみましょう。
 */

 // 問題
 
 // 回答
 SELECT CONCAT(l_name, f_name)
 FROM usr
 WHERE NOT EXISTS
     (
        SELECT *
        FROM rental
        WHERE usr.user_id = rental.user_id
     )