/* 
 * ユーザテーブル（usr）のf_name_kana列の後方に、以下の2列を追加してみましょう。
 * ・sex列（VARCHAR(5)型、デフォルト値は男）
 * ・job列（VARCHAR(30)）型
 */
 
 // 問題

 // 回答
 ALTER TABLE
 usr
 ADD
 sex VARCHAR(5) DEFAULT '男',
 job VARCHAR(30)
 AFTER
 f_name_kana
 ;