/* 
 * 以下は、ユーザテーブル（usr）に対して、住所（都道府県、市町村、その他）で複合インデックスを作成するためのSQL命令ですが、誤りが2点あります。
 * 誤りを指摘してください。
 */
 
 // 問題
 CREATE INDEX
 ind_usr
 IN
 user_id
 (
     prefecture
     city
     o_address
 )
 ;

 // 回答
 CREATE INDEX
 ind_usr
 ON ※「IN」→「ON」に修正
 user_id
 (
     prefecture, ※カンマがない
     city,　※カンマがない
     o_address
 )
 ;